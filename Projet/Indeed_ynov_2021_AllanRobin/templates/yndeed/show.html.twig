{%extends 'base.html.twig' %}

{% block body %}
	<h1>{{offre.Title}}</h1>
	<p>
		<strong>Description</strong>

    <p>{{offre.Description}}</br></p>
    <p>Ville: {{offre.Ville}} </br>   Adresse: {{offre.Adresse}} </br>  Contrat: {{offre.contrat.Name }} </br>Type de contrat: {{offre.typeContrat.name }} </br>
    
    		{% if offre.contrat.Name is same as("CDD") or offre.contrat.Name is same as("FREELANCE")%}
						Fin de la mission: {{offre.FinDeLaMission | date('d/m/Y')}}</p>
						
				{% endif %}
    <p>Dernière mise à jour: {{offre.dateDeMiseAJour | date('d/m/Y')}}   </br> Date de création: {{offre.DateDeCreation | date('d/m/Y')}}</p>
    <a href="{{path('postuler', {'id':offre.id}) }}" class="btn btn-primary">Postuler</a>
	<a href="{{path('edit', {'id':offre.id}) }}" class="btn btn-primary">Modifier</a>
    <a href="{{path('offres')}}" class="btn btn-primary">Retour</a>


{% endblock %}
